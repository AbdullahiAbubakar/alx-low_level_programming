#include "main.h"
/**
 * _abs - returns absolute value of an integer
 * Return: returns zero
 */
int _abs(int n)
{
	int i;
	for (i = 0; i <= 4; i++)
	{
		_putchar(_abs(n(i)));
	}
	_putchar('\n');
	return (0);
}
