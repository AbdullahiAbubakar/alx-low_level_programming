#include "main.h"
/**
 * _islower - checks for lowercase character
 * c - is a characater
 * Return: returns zero
 */
int _islower(int c)
{
	if (_islower(c) != 0)
	{
		return (1);
	}
	else 
	{
		return (0);
	}
}
