#include "main.h"
#include <stdio.h>
#include <string.h>
/**
 * _strlen - length of a string
 * @s: character
 * Return: return zero
 */
int _strlen(char *s)
{
	printf("%ld", strlen(s)- 1);
	return (0);
}
