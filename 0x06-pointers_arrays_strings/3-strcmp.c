#include "main.h"
#include <string.h>
/**
 * _strcmp - compares string
 * @s1: string 1
 * @s2: string 2
 * Return: returns destination string
 */
int _strcmp(char *s1, char *s2)
{
	int val;
	val = strcmp(s1, s2);
	return (val);
}
